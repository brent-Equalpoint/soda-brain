**What & when:** A mono uppercase section label, optionally with a green "See all" action on the right. Use to head blocks on the Home and cockpit panels.

```jsx
<SectionHeader title="People you synced with" action="See all" onAction={goContacts} />
<SectionHeader title="Recent events" />
```
