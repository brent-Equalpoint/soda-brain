**What & when:** An event in the Home. `variant="strip"` for the horizontal "recent events" row; `variant="row"` for the full-width Events-tab list with a logo tile and green met count.

```jsx
<EventCard host="Futureland" name="Creative Meetup" date="Mar 14" met={12} />
<EventCard variant="row" host="Equalpoint" name="Founder Mixer" date="Feb 02" where="Cleveland" met={8} recap />
```
