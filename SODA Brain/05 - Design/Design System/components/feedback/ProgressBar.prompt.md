**What & when:** A thin green fill on a dark track for the one-question survey and stepped flows. Also used as the warmth bar (pass a warmth `color`).

```jsx
<ProgressBar value={3} max={6} />
<ProgressBar ratio={0.82} color="var(--warn)" height={7} />
```
