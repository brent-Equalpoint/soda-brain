**What & when:** A small confirmation pill that rises from the bottom. Green dot + message. Toggle `show` to animate. Use after host/guest actions ("Drop fired", "Card saved").

```jsx
<Toast message="Card saved" show={visible} />
<Toast message="Nudge sent" tone="purple" />
```
